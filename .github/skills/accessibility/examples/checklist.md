# Accessibility Checklist

- Confirm all interactive elements are reachable by keyboard.
- Confirm visible focus state exists for each interactive element.
- Confirm controls have accessible names and clear labels.
- Confirm heading order is logical and not skipped.
- Confirm semantic HTML is used before ARIA overrides.
- Confirm status/error text is announced where required.
- Confirm contrast meets project standard.
