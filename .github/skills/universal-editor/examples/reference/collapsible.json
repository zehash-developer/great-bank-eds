{
  "definitions": [
    {
      "title": "Collapsible",
      "id": "collapsible",
      "plugins": {
        "xwalk": {
          "page": {
            "resourceType": "core/franklin/components/block/v1/block",
            "template": {
              "name": "Collapsible",
              "model": "collapsible",
              "filter": "collapsible"
            }
          }
        }
      }
    }
  ],
  "models": [
    {
      "id": "collapsible",
      "fields": [
        {
          "component": "text",
          "name": "heading",
          "value": "",
          "label": "Heading",
          "valueType": "string",
          "description": "Provide a description. Limit content to 80 characters max",
          "required": true,
          "validation": {
            "maxLength": 80,
            "pattern": "^[^\\s].*$",
            "customErrorMsg": "Heading is required, must not start with a space, and cannot exceed 80 characters"
          }
        },
        {
          "component": "checkbox-group",
          "name": "defaultOpen",
          "label": "Default open state",
          "valueType": "string[]",
          "description": "Select checkbox to reveal collapsible content on page load",
          "options": [
            {
              "name": "",
              "value": "true"
            }
          ]
        },
        {
          "component": "richtext",
          "name": "content",
          "value": "",
          "label": "Collapsible Content",
          "valueType": "string",
          "description": "Add rich text content that will be revealed when the collapsible is opened"
        }
      ]
    }
  ],
  "filters": [
    {
      "id": "collapsible",
      "components": []
    }
  ]
}
